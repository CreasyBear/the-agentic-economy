# Archive

This directory stores intentional non-production material that should stay out of the live website.

- `content-v1/` contains earlier whitepaper drafts.
- `image-sources/` contains original large PNG hero renders kept for editing or regeneration.
- `reference/` contains screenshots and other non-deployed reference material.

Nothing in this directory should be linked from navigation or treated as deploy-ready content.
